import React, { Component } from 'react';
import { ColorBars } from './components';

export default class App extends Component {
    render() {
        return (
            <ColorBars />
        );
    }
}
